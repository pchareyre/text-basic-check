[project]
name = "text-basic-check"
version = "0.1.0"
description = ""
authors = [
    {name = "Patrick Chareyre",email = "39535769+pchareyre@users.noreply.github.com"}
]
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pydantic (>=2.12.5,<3.0.0)",
    "symspellpy (>=6.9.0,<7.0.0)",
    "setuptools (>=80.9.0,<81.0.0)",
    "pyyaml (>=6.0.3,<7.0.0)"
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"

[dependency-groups]
dev = [
    "pytest (>=9.0.2,<10.0.0)"
]
